---

const { title, id } =  Astro.props
---

<section id={id}>
	<h2>{title}</h2>
	<slot />
</section>

<style>
	h2 {
		margin: 0 0 2rem;
		font-size: 2rem;
		font-weight: 300;
		color: var(--text-color-light);
	}
</style>